<Window x:Class="ClaudeUsageTray.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
    <!-- Same borderless rounded-Border-with-shadow shape as AppDialogWindow, since this is a similarly lightweight, non-resizable secondary window. -->
    <Window.Resources>
        <Style x:Key="AboutCloseButton" TargetType="Button">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="13" Background="Transparent">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                       Foreground="{DynamicResource MaterialDesignBody}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#14808080" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CheckUpdateButton" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Background="Transparent">
                            <TextBlock x:Name="Glyph" Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="{DynamicResource MaterialDesignBody}" Opacity="0.7"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#14808080" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Glyph" Property="Opacity" Value="0.3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PathBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="11.5" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Cursor" Value="IBeam" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="14" Padding="26" Width="400">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Opacity="0.28" Color="Black" />
            </Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image x:Name="AppIcon" Width="40" Height="40" Margin="0,0,14,0" VerticalAlignment="Center" />
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="AppNameText" FontSize="16" FontWeight="Medium" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center" />
                                <Button x:Name="CheckUpdateBtn" Style="{StaticResource CheckUpdateButton}"
                                        Click="OnCheckUpdateClick" />
                            </StackPanel>
                            <TextBlock x:Name="VersionText" FontSize="12.5" Opacity="0.65" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,2,0,0" />
                        </StackPanel>
                    </StackPanel>
                    <Button x:Name="CloseButton" Style="{StaticResource AboutCloseButton}"
                            HorizontalAlignment="Right" VerticalAlignment="Top" Click="OnCloseClick" />
                </Grid>

                <TextBlock x:Name="PathLabel" FontSize="11" Opacity="0.55" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,0,0,4" />
                <Border Background="{DynamicResource MaterialDesignDivider}" CornerRadius="6" Padding="10,7" Margin="0,0,0,20">
                    <TextBox x:Name="PathText" Style="{StaticResource PathBox}" />
                </Border>

                <TextBlock x:Name="ChangelogLabel" FontSize="12" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource MaterialDesignBody}" />
                <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="ChangelogHost" />
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</Window>
