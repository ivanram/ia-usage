<Window x:Class="ClaudeUsageTray.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        Deactivated="OnDeactivated">
    <Border x:Name="RootBorder"
            CornerRadius="14"
            Background="{DynamicResource MaterialDesignPaper}"
            RenderTransformOrigin="0.5,0.5"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.25" Color="Black" />
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform x:Name="EntranceTransform" Y="12" />
        </Border.RenderTransform>
        <Border.Resources>
            <Storyboard x:Key="EntranceStoryboard">
                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                  From="0" To="1" Duration="0:0:0.18" />
                <DoubleAnimation Storyboard.TargetName="EntranceTransform"
                                  Storyboard.TargetProperty="Y"
                                  From="12" To="0" Duration="0:0:0.22">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Border.Resources>
        <StackPanel x:Name="ContentHost" Margin="20" MinWidth="280" />
    </Border>
</Window>
