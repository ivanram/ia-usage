# Build output
bin/
obj/
publish/
publish_tmp/
publish_fx/
publish_sc/

# Packaged EXEs are distributed as GitHub Release assets, not repo content
Releases/

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo

# Runtime-generated files (never contain secrets, but are pure noise)
status.txt
crash.txt
*_debug.txt

# User settings (token lives only in %LOCALAPPDATA%, never in the repo,
# but ignore just in case a settings.json ever ends up next to the exe)
settings.json
